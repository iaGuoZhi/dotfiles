[user]
	email = qtxuning1999@sjtu.edu.cn
	name = Guo Zhi
[credential]
	helper = store
[sendemail]
	smtpuser = qtxuning1999@sjtu.edu.cn
	smtpserver = smtp.sjtu.edu.cn
	smtpencryption = tls
	smtpserverport = 587
	confirm = auto
[sendemail.linux]
	tocmd ="`pwd`/scripts/get_maintainer.pl --nogit --nogit-fallback --norolestats --nol"
	cccmd ="`pwd`/scripts/get_maintainer.pl --nogit --nogit-fallback --norolestats --nom"
